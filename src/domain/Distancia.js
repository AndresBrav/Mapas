export class Distancia {
    constructor({ origin, destination, distance, duration }) {
        this.origin = origin;
        this.destination = destination;
        this.distance = distance;
        this.duration = duration;
    }
}
